import '../styles/addNewButton.css';

const AddNewButton = () => {
    return (
        <>
            <button className="addNewBtn"> Add New</button>
        </>
    );
};

export default AddNewButton;
