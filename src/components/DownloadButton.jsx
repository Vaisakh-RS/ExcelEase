import '../styles/addNewButton.css';

const DownloadButton = () => {
    return (
        <>
            <div className="buttonDU">
                <button onClick={() => {}} className="upload">
                    Upload New
                </button>
                <button onClick={() => {}} className="download">
                    Download
                </button>
            </div>
        </>
    );
};

export default DownloadButton;
